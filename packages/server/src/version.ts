/**
 * Версия сервера.
 *
 * Отдельным файлом, потому что её спрашивают двое: `main.ts` печатает её при
 * запуске, а `server.ts` отдаёт в ответе на пробу живости. Импортировать её из
 * `main.ts` серверу нельзя — тот при загрузке разбирает переменные окружения и
 * поднимает сокет.
 *
 * При выпуске это число меняется вместе с версией в package.json; что они
 * совпадают, проверяется отдельно.
 */
export const VERSION = "0.28.0";
